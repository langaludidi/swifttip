import Link from "next/link";
import { AppMark } from "@/components/AppMark";

export default function VenuePage(){return <main className="dashboard-shell"><header className="dashboard-topbar"><div className="brand-lockup"><AppMark size={42}/><strong>SwiftTip Venue</strong></div><Link className="action-link" href="/">Customer view</Link></header><section className="dashboard-title"><span className="eyebrow">Riverside Service Station</span><h1>Venue overview</h1><p className="lead">Lightweight participation: confirm workers, see aggregate activity, and avoid payroll or payout administration.</p></section><div className="metric-grid" style={{marginTop:22}}><article className="metric-card"><span>Active workers</span><strong>12</strong></article><article className="metric-card"><span>Tips this week</span><strong>48</strong></article><article className="metric-card"><span>Gross gratuity value</span><strong>R1,260</strong></article><article className="metric-card"><span>Needs confirmation</span><strong>1</strong></article></div><section className="dashboard-section"><h2>Workers</h2>{[['Thando','Fuel Attendant','Active'],['Nomsa','Cashier','Active'],['Sipho','Fuel Attendant','Confirm']].map(([name,role,state])=><div className="list-row" key={name}><div><strong>{name}</strong><div className="meta">{role}</div></div><span className={state==='Active'?'status-chip success':'status-chip warning'}>{state}</span></div>)}</section><section className="dashboard-section"><h2>Venue boundary</h2><p className="lead">This interface intentionally excludes worker banking, KYC documents, settlement control, payroll, tip pools and worker performance rankings.</p></section></main>}
