import Link from "next/link";
import { formatZar, calculateTipPricing } from "@/lib/money";

export default function WorkerOnboardingPage(){
  const example=calculateTipPricing(5000);
  return <main className="flow-shell"><div className="flow-page"><header className="simple-header"><Link className="back-link" href="/worker">←</Link><strong>Worker onboarding</strong><span style={{width:42}}/></header><section className="tip-flow"><span className="eyebrow">Step 1 of 5</span><h1>Get ready to receive verified digital tips.</h1><p className="lead">Activation is gated by identity, venue association, provider settlement readiness and accepted commercial terms.</p><div className="dashboard-section" style={{marginTop:22}}><h2>Activation checklist</h2>{[['Your details','Ready'],['Identity verification','Pending'],['Venue confirmation','Pending'],['Payment details','Pending'],['Commercial terms','Review']].map(([a,b],i)=><div className="list-row" key={a}><div><strong>{i+1}. {a}</strong></div><span className={b==='Ready'?'status-chip success':'status-chip warning'}>{b}</span></div>)}</div><div className="dashboard-section"><span className="eyebrow">Transparent pricing</span><h2 style={{marginTop:7}}>No signup or monthly fee.</h2><p className="lead">The current working hypothesis is a 5% SwiftTip success fee when you receive a tip.</p><div className="money-breakdown"><div className="money-row"><span>Example gross tip</span><strong>{formatZar(5000)}</strong></div><div className="money-row"><span>SwiftTip success fee</span><strong>− {formatZar(example.workerFeeCents)}</strong></div><div className="money-row total"><span>Your amount</span><strong>{formatZar(example.workerNetCents)}</strong></div></div></div><p className="prototype-warning">Actual worker activation remains unavailable until the new database, provider KYC/settlement requirements and approved worker terms are configured.</p></section></div></main>;
}
